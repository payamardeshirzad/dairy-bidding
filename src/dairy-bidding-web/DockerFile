# build
FROM node:22-alpine AS build
WORKDIR /app
COPY package*.json ./
RUN npm ci
COPY . .
ARG VITE_IDENTITY_BASE_URL=http://localhost:5245
ARG VITE_BIDDING_BASE_URL=http://localhost:5170
ARG VITE_AUTH_STORAGE_MODE=localStorage
ENV VITE_IDENTITY_BASE_URL=$VITE_IDENTITY_BASE_URL
ENV VITE_BIDDING_BASE_URL=$VITE_BIDDING_BASE_URL
ENV VITE_AUTH_STORAGE_MODE=$VITE_AUTH_STORAGE_MODE
RUN npm run build

# runtime
FROM nginx:1.27-alpine
COPY nginx.conf /etc/nginx/conf.d/default.conf
COPY --from=build /app/dist /usr/share/nginx/html
EXPOSE 80